# Fast Requirements for Docker Build - Only Essential Dependencies
# This reduces build time by excluding heavy optional dependencies

# Core web framework
fastapi==0.95.2
uvicorn[standard]==0.22.0

# Essential ML libraries (lightweight)
scikit-learn==1.3.0
pandas==2.0.3
numpy==1.23.5

# API and data fetching
requests>=2.31.0
aiohttp==3.8.5

# Environment and configuration
python-dotenv==1.0.0
pydantic==1.10.8

# Basic monitoring
structlog==23.1.0

# Note: Heavy dependencies like TensorFlow, matplotlib, etc. are excluded
# for faster builds. Add them back in production if needed.
